#include <QApplication>
#include <QMainWindow>


int main(int argc, char *argv[]){
    QApplication app(argc, argv);

    QMainWindow win;
    win.setCentralWidget(  );
    win.resize(400,300);
    win.show();
   // Dialog w = new Dialog();
   // w.show();
   // int result = app.exec();
    printf ( "*** start ***\r\n" );
    return 0;
}
